@require 'nib'
@require 'zorium-paper/base'

json('zorium-paper/colors.json')

.z-home
  > .title
    zp-font-headline()
    margin: 26px 0 16px 0

  > .content
    > .text
      max-width: 440px
      > .a
        color: $blueGrey500

      > .buttons
        padding-top: 20px
