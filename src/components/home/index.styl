@require 'nib'
@require 'zorium-paper/base'

json('zorium-paper/colors.json')

.z-home
  > .title
    zp-font-headline()

  > .content
    > .text
      max-width: 440px
      > .a
        color: $blueGrey500

      > .buttons
        padding-top: 20px
